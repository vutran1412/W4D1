<%= form_with(model: artwork_share, local: true) do |form| %>
  <% if artwork_share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artwork_share.errors.count, "error") %> prohibited this artwork_share from being saved:</h2>

      <ul>
      <% artwork_share.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
